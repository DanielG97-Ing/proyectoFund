function modal (db, printProducts) {
    // console.log(db)
    const modal = document.querySelector('.product')

    modal.addEventListener('click', function(e){
        if(e.target.closest('.'))
    })
}

export default modal